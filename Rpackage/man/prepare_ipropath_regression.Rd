% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prepare_ipropath_regression.R
\name{prepare_ipropath_regression}
\alias{prepare_ipropath_regression}
\title{Prepare regression results for iProPath}
\usage{
prepare_ipropath_regression(
  x_df_list,
  fixed_cov,
  mediator_list,
  y_covdf_list,
  Y_list,
  x_name = "mutation",
  y_name,
  num_cores = 1L,
  adjust_methods = c("BH", "bonferroni", "BY")
)
}
\arguments{
\item{x_df_list}{List of X data frames (as used by your pipeline).}

\item{fixed_cov}{Fixed covariates data frame/matrix (as used by your pipeline).}

\item{mediator_list}{List of mediators (as used by your pipeline).}

\item{y_covdf_list}{List of Y covariates data frames (as used by your pipeline).}

\item{Y_list}{List of outcome data frames (as used by your pipeline).}

\item{x_name}{Character string specifying X type/name (e.g., \code{"mutation"}).}

\item{y_name}{Character string specifying Y type/name (e.g., \code{"glyco"}).}

\item{num_cores}{Integer number of CPU cores to use.}

\item{adjust_methods}{Character vector of p-value adjustment methods to compute
for pairwise results.}
}
\value{
A list with components:
\describe{
\item{merged_df}{Data frame of merged trans-regression results.}
\item{y_transreg}{Data frame of Y trans-regression results (pre-merge).}
\item{m_reg}{Data frame of M regression results (pre-merge).}
\item{pairwise_res}{Data frame of pairwise regression results with adjusted p-values.}
}
}
\description{
Runs trans regression and pairwise regression, then returns the merged
regression table used by iProPath as well as pairwise regression results.
}
